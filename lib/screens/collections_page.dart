import 'package:flutter/material.dart';

class collections_page extends StatelessWidget {
  const collections_page({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
