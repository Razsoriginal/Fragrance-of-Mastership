import 'package:flutter/material.dart';

class cat_science extends StatelessWidget {
  const cat_science({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
